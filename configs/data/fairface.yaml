name: fairface
root: data

train_split: train
val_split: val

target_attr: race
# Assumes the extracted FairFace files live under the same dataset root used for
# STL10/DTD/Food101, e.g. data/fairface_label_train.csv and
# data/fairface-img-margin025-trainval/.
image_dirname: fairface-img-margin025-trainval
train_csv: fairface_label_train.csv
val_csv: fairface_label_val.csv
csv_dir: null
image_root: null
download: false

batch_size: 256
num_workers: 4
pin_memory: true
prefetch_factor: 2
persistent_workers: true
